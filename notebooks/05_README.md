# Directed GNN network-flow example

The main implementation is `05_directed_gnn_network_flow.ipynb`.

It compares direction-aware message passing with an intentionally symmetrized baseline and evaluates both as candidate-arc screening policies for min-cost flow.
