# Additional notes

Direction is semantic in flow models: `u -> v` and `v -> u` are distinct variables. Symmetrization is included only as a controlled baseline that deliberately discards this information.
